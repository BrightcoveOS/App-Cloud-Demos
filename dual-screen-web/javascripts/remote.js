// update the block color. this function is passed from local.html to remote.html via
// messageBroker.js

function updateColor(color) {
    document.getElementById("block").style.backgroundColor = color;
}
